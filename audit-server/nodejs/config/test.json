{
	"audit" : {
		"type" : "FILE", 
		"FILE" : {
			"logdir" : "/tmp",
			"filename" : "canCreateAnAuditlogFile.log"
		},
		"SYSLOG" : {
		    "host": "localhost", //The host running syslogd.
		    "port": 514, //The port on the host that syslog is running on.
		    "facility" : "local5", //Syslog facility to use.
		    "tag": "hadoop-auditserver" //The type of the application tag to use.
		}
	}
}